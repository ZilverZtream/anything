name: Build and Test

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder build
        run: |
          echo "No CMake project is configured yet."
          echo "Skipping build and test steps until the toolchain is available."
